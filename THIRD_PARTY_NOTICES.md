# Source attribution

Public contract snapshots come from `ores-otel/ores-interfaces`, copyright 2026
OreSoftware, MIT license. The exact revision is in `shared-interfaces.json` and
every built `provenance.json`; the original license ships as `LICENSE.upstream`.
These are downstream source distributions, not newly authored copies.

Build-time parity and evidence verification use
`ORESoftware/typespec-json-schema-validator` at the manifest's immutable commit.
Its implementation and dependencies are not bundled into the interface package.
